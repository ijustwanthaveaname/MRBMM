% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonization.R
\name{harmonize_for_mr}
\alias{harmonize_for_mr}
\title{Complete GWAS Harmonization Workflow}
\usage{
harmonize_for_mr(
  exp_gwas,
  otc_gwas,
  exp_name,
  otc_name,
  plink_bin,
  ref_panel,
  pval_threshold = 5e-08,
  clump_kb = 10000,
  clump_r2 = 0.001,
  use_proxy = TRUE,
  proxy_r2 = 0.8,
  temp_dir = tempdir(),
  verbose = TRUE
)
}
\arguments{
\item{exp_gwas}{Exposure GWAS data frame (from read_gwas_data)}

\item{otc_gwas}{Outcome GWAS data frame (from read_gwas_data)}

\item{exp_name}{Exposure trait name}

\item{otc_name}{Outcome trait name}

\item{plink_bin}{Path to plink executable (e.g., "/usr/bin/plink")}

\item{ref_panel}{Path to reference panel WITHOUT extension (e.g., "/data/1kg_eur/EUR")}

\item{pval_threshold}{P-value threshold for IV selection (default: 5e-8)}

\item{clump_kb}{Clumping window in kb (default: 10000)}

\item{clump_r2}{Clumping r² threshold (default: 0.001)}

\item{use_proxy}{Whether to search for proxy SNPs (default: TRUE)}

\item{proxy_r2}{Minimum r² for proxy SNPs (default: 0.8)}

\item{temp_dir}{Temporary directory for plink output (default: tempdir())}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
Harmonized data frame from TwoSampleMR::harmonise_data()
}
\description{
Performs complete harmonization matching real data evaluation:
1. Read GWAS data
2. Filter exposure SNPs by p-value
3. Format for TwoSampleMR
4. LD clumping with local reference panel
5. Proxy SNP search with local plink
6. Harmonization
}
\examples{
\dontrun{
# Read GWAS data
exposure <- read_gwas_data("exposure.txt", ...)
outcome <- read_gwas_data("outcome.txt", ...)

# Harmonize
harmonized <- harmonize_for_mr(
  exp_gwas = exposure,
  otc_gwas = outcome,
  exp_name = "BMI",
  otc_name = "CAD",
  plink_bin = "/usr/bin/plink",
  ref_panel = "/data/1kg_eur/EUR"
)

# Extract for BMM
bmm_data <- extract_bmm_data(harmonized)
}
}
